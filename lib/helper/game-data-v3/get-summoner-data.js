"use strict";

/**
 * @param summonerIds an array of summonerIds
 * @param region
 * Return an object in the callback,
 * keys are summonerIds,
 * values an object with summoner details (rank, last season rank, level, ...)
 */
module.exports = function getSummonerData(summonerIds, region, cb) {
  async.auto
};
